const modelos = {
  medicos: require('../../models/medico.models'),
  usuarios: require('../../models/usuario.models'),
  hospitales: require('../../models/hospital.models'),
};

module.exports = {
  modelos,
};
